Screen {
    layout: vertical;
}

#main-split {
    layout: horizontal;
    height: 1fr;
}

#left-pane {
    width: 3fr;
    layout: vertical;
}

/* ── Session Table (primary) ── */
#session-table {
    height: 2fr;
    margin: 0 1;
    border: double $primary;
    border-title-color: $text;
    border-title-style: bold;
    border-title-background: $primary 20%;
    border-subtitle-color: $text-muted;
    border-subtitle-style: italic;
    padding: 0 1;
}

/* ── Permission/Waiting Panel (secondary) ── */
#permission-panel {
    height: 1fr;
    min-height: 5;
    margin: 0 1;
    border: heavy $surface-lighten-2;
    border-title-color: $text-muted;
    border-title-style: bold;
}

/* When sessions are actually waiting, promote to primary-style */
#permission-panel.has-waiting {
    border: double $warning;
    border-title-color: $warning;
    border-title-style: bold;
    border-title-background: $warning 15%;
}

PermissionPanel .urgent {
    background: $warning 15%;
}

#no-permissions {
    color: $text-muted;
    text-style: italic;
    padding: 1 2;
}

/* ── Activity Log (secondary) ── */
#activity-log {
    height: 1fr;
    margin: 0 1;
    border: heavy $surface-lighten-2;
    border-title-color: $text-muted;
    border-title-style: bold;
    padding: 0 1;
}

/* ── Detail Panel (primary) ── */
#session-detail {
    width: 2fr;
    min-width: 35;
    margin: 0 1 0 0;
    padding: 1 2;
    border: double $primary;
    border-title-color: $text;
    border-title-style: bold;
    border-title-background: $primary 20%;
    overflow-y: auto;
}
